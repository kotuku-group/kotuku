<?xml version="1.0"?>
<?xml-stylesheet type="text/xsl" href="class.xsl"?>

<book>
  <info>
    <name>GradientConic</name>
    <type>class</type>
    <module>Vector</module>
    <comment>Conic colour gradient paint server.</comment>
    <version>1</version>
    <id>9fd5612c</id>
    <idstring>GRADIENTCONIC</idstring>
    <category>Graphics</category>
    <copyright>Paul Manias © 2010-2026</copyright>
    <description>
<p>GradientConic draws colour values around a centre point, producing a conic gradient.</p>
<p>The <fl>Span</fl> field is honoured by <code>REPEAT</code>, <code>REFLECT</code> and <code>CLIP</code> spread modes.  A <code>REFLECT</code> spread sweeps the colour ramp forwards and then back again within each Span cycle, forming a triangular sweep.  With the default span of <code>1.0</code> a single triangle spans the full turn: the ramp runs from start to end across the first half-turn and back to the start across the second.</p></description>
    <source>
      <file path="painters/">gradient_conic.cpp</file>
    </source>
  </info>

  <fields>
    <field>
      <name>CX</name>
      <comment>The horizontal centre point of the gradient.</comment>
      <access read="R" write="S">Read/Set</access>
      <type>UNIT</type>
      <description>
<p>The <code>(CX, CY)</code> coordinates define the centre point around which the conic gradient rotates.  By default, the centre point is set to <code>50%</code>.</p>
      </description>
    </field>

    <field>
      <name>CY</name>
      <comment>The vertical centre point of the gradient.</comment>
      <access read="R" write="S">Read/Set</access>
      <type>UNIT</type>
      <description>
<p>The <code>(CX, CY)</code> coordinates define the centre point around which the conic gradient rotates.  By default, the centre point is set to <code>50%</code>.</p>
      </description>
    </field>

    <field>
      <name>Radius</name>
      <comment>The radius of the gradient.</comment>
      <access read="R" write="S">Read/Set</access>
      <type>UNIT</type>
      <description>
<p>The radius of the conic gradient can be defined as a fixed unit or scaled relative to its container.  A default radius of 50% applies if this field is not set.</p>
      </description>
    </field>

    <field>
      <name>Span</name>
      <comment>Defines the angular size of one conic gradient cycle.</comment>
      <access read="R" write="S">Read/Set</access>
      <type>DOUBLE</type>
      <description>
<p>The span is normalised so that <code>1.0</code> equals 360 degrees.  Values smaller than <code>1.0</code> define a partial angular cycle for <class name="Gradient" field="SpreadMethod">Gradient.SpreadMethod</class> modes that can repeat, reflect or clip the gradient.  The default value is <code>1.0</code>.</p>
<p>When <class name="Gradient" field="SpreadMethod">Gradient.SpreadMethod</class> is <code>PAD</code>, Span is ignored and the conic gradient renders as a full-turn gradient.</p>
      </description>
    </field>

    <field>
      <name>XMLDef</name>
      <comment>Returns an SVG compliant XML string that describes the effect.</comment>
      <access read="G">Get</access>
      <type>STRING</type>
      <tags>caller-owns-result</tags>
    </field>

  </fields>
  <structs>
  </structs>
</book>
