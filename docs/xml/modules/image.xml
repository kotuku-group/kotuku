<?xml version="1.0"?>
<?xml-stylesheet type="text/xsl" href="module.xsl"?>

<book>
  <info>
    <name>Image</name>
    <type>module</type>
    <version>1</version>
    <copyright>Paul Manias © 2001-2026</copyright>
    <classes>
      <class>Image</class>
    </classes>
  </info>

  <types>
    <constants lookup="PCF" comment="Flags for the Image class.">
      <const name="ALPHA">If an image supports an alpha channel, this field will be set (if you will be saving a Image with an alpha channel, set this flag prior to initialisation and build the channel using the object provided in the <fl>Mask</fl> field).</const>
      <const name="FORCE_ALPHA_32">If the image source includes an alpha channel mask, it will normally be separated out and stored in the <fl>Mask</fl> field.  If you set the <code>FORCE_ALPHA_32</code> option before loading, the image will be forced into 32-bit graphics mode with alpha channel.  The mask information will be stored in the alpha byte and the <fl>Mask</fl> field will not be set.</const>
      <const name="LAZY">Prevents automatic loading of the image.  Use the Query action to process the image header or Activate to load the image data.</const>
      <const name="MASK">A mask has been generated for the image.</const>
      <const name="NEW">Indicates that the image will be created from scratch.</const>
      <const name="NO_PALETTE">Automatically upgrade palette based images to 32 bit colour.</const>
      <const name="SCALABLE">This read-only flag is automatically set if the image source is scalable (such as SVG).</const>
    </constants>

  </types>
  <structs>
  </structs>
</book>
