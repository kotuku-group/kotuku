<?xml version="1.0"?>
<?xml-stylesheet type="text/xsl" href="module.xsl"?>

<book>
  <info>
    <name>Audio</name>
    <type>module</type>
    <comment>Sample mixing and playback system with a floating-point mixer and platform audio output.</comment>
    <version>1</version>
    <copyright>Paul Manias © 2002-2026</copyright>
    <description>
<p>The Audio module manages the audio pipeline from sample loading through to hardware output.  It provides three class interfaces:</p>
<list type="bullet">
<li><class name="Sound">Sound</class>: High-level sample playback.  Loads WAVE files, manages its own resources and decides whether a sample is
played from memory or streamed.  This is the recommended interface for most applications.</li>
<li><class name="Audio">Audio</class>: Low-level mixer and device interface.  Provides channel sets, sample buffers and streams, command
sequencing, and control over output rate, bit depth, buffering and mixing quality.</li>
<li><class name="AudioEffect">AudioEffect</class>: Base class for DSP processors that attach to a channel set or to the global mix of an <class name="Audio">Audio</class>
object.</li>
</list>
<p>The internal mixer processes all audio as 32-bit floating-point data regardless of the output bit depth.  The mixer supports:</p>
<list type="bullet">
<li>Linear interpolation between sample frames when <code>ADF::OVER_SAMPLING</code> is enabled, reducing aliasing when samples are
played at rates that differ from the output rate.</li>
<li>Volume ramping to avoid clicks when channel volumes change, enabled by <code>ADF::VOL_RAMPING</code> in combination with
<code>ADF::OVER_SAMPLING</code>.</li>
<li>Two levels of low-pass output smoothing, selected with <code>ADF::FILTER_LOW</code> or <code>ADF::FILTER_HIGH</code>.  The <class name="Audio" field="Quality">Audio.Quality</class>
field configures the filtering and oversampling flags as a group.</li>
<li>Unidirectional and bidirectional sample looping, including a secondary loop region.</li>
<li>Per-channel and global effect chains built from <class name="AudioEffect">AudioEffect</class> processors.</li>
</list>
<p>Output behaviour differs by platform:</p>
<list type="bullet">
<li><b>Linux (ALSA):</b> All playback, including <class name="Sound">Sound</class> objects, is mixed by the internal mixer and written by a
dedicated worker thread.  Buffering is configured with the <class name="Audio" field="Periods">Audio.Periods</class> and <class name="Audio" field="PeriodSize">Audio.PeriodSize</class> fields, and the
ALSA mixer is used for system volume control.</li>
<li><b>Windows (WASAPI):</b> All Sounds use the internal mixer and its application/global effects.  Each active
Audio object owns an event-driven shared-mode stream at the endpoint mix rate.  IAudioClient3 uses the supported
default period; event-driven IAudioClient is the compatibility fallback.  Windows 10 or later is supported.</li>
</list>
<p>The <class name="Sound" field="Stream">Sound.Stream</class> field determines whether a sample is loaded into memory or streamed from its source.  With <code>STREAM::SMART</code>, samples larger than 256KB are streamed; with <code>STREAM::ALWAYS</code>, samples larger than 16KB are streamed; with <code>STREAM::NEVER</code>, the sample is always memory-resident.  Loop points are preserved when a sample is streamed.</p>
<p>Technical specifications:</p>
<list type="bullet">
<li>Internal processing: 32-bit floating-point.</li>
<li>Output formats: 8-bit and 16-bit integer, and 32-bit floating-point.</li>
<li>Output rates: Up to 192 kHz, subject to hardware negotiation.  Windows follows the endpoint mix rate.</li>
<li>Channel configurations: Mono and stereo output.  Mono and stereo samples are converted to the output layout during
mixing.</li>
</list></description>
    <source>
      <file path="src/audio/">audio.cpp</file>
      <file path="src/audio/">commands.cpp</file>
    </source>
    <classes>
      <class>Audio</class>
      <class>AudioEffect</class>
      <class>AudioEqualiser</class>
      <class>Sound</class>
    </classes>
  </info>

  <function>
    <name>MixContinue</name>
    <comment>Resumes playback of a paused channel.</comment>
    <prototype>ERR snd::MixContinue(objAudio * Audio, INT Handle)</prototype>
    <tiriPrototype>err = MixContinue(Audio:obj, Handle:num)</tiriPrototype>
    <tags>mutates-object</tags>
    <input>
      <param type="objAudio *" name="Audio">The target Audio object.</param>
      <param type="INT" name="Handle">The target channel.</param>
    </input>
    <description>
<p>This function resumes a channel that was paused by <function>MixPause</function>.  Playback continues from the paused position with the same loop phase, direction and release state.  If the note was released before or during the pause, it remains released.</p>
<p>Only paused channels are affected.  Calling this function on a channel that is playing, released, stopped by <function>MixStop</function> or finished has no effect and returns <code>Okay</code>.  Use <function>MixPlay</function> to start a new playback after a stop or natural completion.</p>
<p>On worker backends, <code>Okay</code> reports queue admission; the command is applied before a subsequent render period.</p>
    </description>
    <result type="ERR">
      <error code="Okay">Operation successful.</error>
      <error code="OutOfRange">A value is outside of the valid range</error>
      <error code="BufferOverflow">A buffer overflow has occurred</error>
      <error code="NotInitialised">The resource has not been initialised</error>
      <error code="NullArgs">Function call missing argument value(s)</error>
    </result>
  </function>

  <function>
    <name>MixFrequency</name>
    <comment>Sets a channel's playback rate.</comment>
    <prototype>ERR snd::MixFrequency(objAudio * Audio, INT Handle, INT Frequency)</prototype>
    <tiriPrototype>err = MixFrequency(Audio:obj, Handle:num, Frequency:num)</tiriPrototype>
    <tags>mutates-object</tags>
    <input>
      <param type="objAudio *" name="Audio">The target Audio object.</param>
      <param type="INT" name="Handle">The target channel.</param>
      <param type="INT" name="Frequency">The desired frequency.</param>
    </input>
    <description>
<p>Use this function to set the playback rate of a mixer channel.</p>
    </description>
    <result type="ERR">
      <error code="Okay">Operation successful.</error>
      <error code="Failed">General failure</error>
      <error code="OutOfRange">A value is outside of the valid range</error>
      <error code="NullArgs">Function call missing argument value(s)</error>
    </result>
  </function>

  <function>
    <name>MixMute</name>
    <comment>Mutes the audio of a channel.</comment>
    <prototype>ERR snd::MixMute(objAudio * Audio, INT Handle, INT Mute)</prototype>
    <tiriPrototype>err = MixMute(Audio:obj, Handle:num, Mute:num)</tiriPrototype>
    <tags>mutates-object</tags>
    <input>
      <param type="objAudio *" name="Audio">The target Audio object.</param>
      <param type="INT" name="Handle">The target channel.</param>
      <param type="INT" name="Mute">Set to true to mute the channel.  A value of 0 will undo the mute setting.</param>
    </input>
    <description>
<p>Use this function to mute the audio of a mixer channel.</p>
    </description>
    <result type="ERR">
      <error code="Okay">Operation successful.</error>
      <error code="OutOfRange">A value is outside of the valid range</error>
      <error code="NullArgs">Function call missing argument value(s)</error>
    </result>
  </function>

  <function>
    <name>MixPan</name>
    <comment>Sets a channel's panning value.</comment>
    <prototype>ERR snd::MixPan(objAudio * Audio, INT Handle, DOUBLE Pan)</prototype>
    <tiriPrototype>err = MixPan(Audio:obj, Handle:num, Pan:num)</tiriPrototype>
    <tags>mutates-object</tags>
    <input>
      <param type="objAudio *" name="Audio">The target Audio object.</param>
      <param type="INT" name="Handle">The target channel.</param>
      <param type="DOUBLE" name="Pan">The desired pan value between -1.0 and 1.0.</param>
    </input>
    <description>
<p>Use this function to set a mixer channel's panning value.  Accepted values are between -1.0 (left) and 1.0 (right).</p>
    </description>
    <result type="ERR">
      <error code="Okay">Operation successful.</error>
      <error code="NullArgs">Function call missing argument value(s)</error>
    </result>
  </function>

  <function>
    <name>MixPause</name>
    <comment>Suspends playback on a channel without ending it.</comment>
    <prototype>ERR snd::MixPause(objAudio * Audio, INT Handle)</prototype>
    <tiriPrototype>err = MixPause(Audio:obj, Handle:num)</tiriPrototype>
    <tags>mutates-object</tags>
    <input>
      <param type="objAudio *" name="Audio">The target Audio object.</param>
      <param type="INT" name="Handle">The target channel.</param>
    </input>
    <description>
<p>This function suspends a playing or released channel.  The playback position, loop phase, direction and release state are preserved, and <function>MixContinue</function> resumes from exactly that point.  No <code>OnStop</code> notification is delivered, because the playback has not ended.</p>
<p>Pausing a channel that is already paused, stopped or finished has no effect and returns <code>Okay</code>.  While paused, a channel can still be released with <function>MixRelease</function>, which takes effect on resumption (or ends the note immediately for loop modes where release ends it).  <function>MixStop</function> terminates a paused playback, and <function>MixPlay</function> replaces it.</p>
<p>Streams continue to prefetch source data while paused.</p>
<p>On worker backends, <code>Okay</code> reports queue admission; the command is applied before a subsequent render period.</p>
    </description>
    <result type="ERR">
      <error code="Okay">Operation successful.</error>
      <error code="OutOfRange">A value is outside of the valid range</error>
      <error code="BufferOverflow">A buffer overflow has occurred</error>
      <error code="NotInitialised">The resource has not been initialised</error>
      <error code="NullArgs">Function call missing argument value(s)</error>
    </result>
  </function>

  <function>
    <name>MixPlay</name>
    <comment>Commences channel playback at a set frequency.</comment>
    <prototype>ERR snd::MixPlay(objAudio * Audio, INT Handle, INT64 Position)</prototype>
    <tiriPrototype>err = MixPlay(Audio:obj, Handle:num, Position:num)</tiriPrototype>
    <tags>mutates-object</tags>
    <input>
      <param type="objAudio *" name="Audio">The target Audio object.</param>
      <param type="INT" name="Handle">The target channel.</param>
      <param type="INT64" name="Position">The new playing position, measured in bytes.  It must be aligned to a complete source frame.</param>
    </input>
    <description>
<p>This function will start playback of the sound sample associated with the target mixer channel.  Playback always starts in the first loop, unreleased.  This applies to every prior channel state: playing, released, paused, stopped and finished.  Replacing an active playback does not deliver the sample's <code>OnStop</code> notification for the replaced playback.</p>
<p>For streams, a new playback also cancels any earlier <function>MixRelease</function>, so a configured stream loop is honoured again.</p>
    </description>
    <result type="ERR">
      <error code="Okay">Playback successfully initiated.</error>
      <error code="Args">Position is not frame-aligned.</error>
      <error code="NoData">The referenced sample is unconfigured.</error>
      <error code="OutOfRange">Position exceeds sample boundaries.</error>
      <error code="FieldNotSet">Channel not associated with a valid sample.</error>
      <error code="BufferOverflow">The worker command queue is full.</error>
      <error code="NotInitialised">The audio worker is stopping.</error>
      <error code="NullArgs">Required parameters are null or missing.</error>
    </result>
  </function>

  <function>
    <name>MixRelease</name>
    <comment>Releases the note playing on a channel.</comment>
    <prototype>ERR snd::MixRelease(objAudio * Audio, INT Handle)</prototype>
    <tiriPrototype>err = MixRelease(Audio:obj, Handle:num)</tiriPrototype>
    <tags>mutates-object</tags>
    <input>
      <param type="objAudio *" name="Audio">The target Audio object.</param>
      <param type="INT" name="Handle">The target channel.</param>
    </input>
    <description>
<p>This function signals a note release (a 'key-off') to a playing or paused channel.  The effect depends on the loop mode of the channel's sample, as described by the <lk>LOOP</lk> constants:</p>
<list type="bullet">
<li><code>SINGLE_RELEASE</code>: Playback leaves the loop when it next reaches the loop end, then plays the remaining sample
data.</li>
<li><code>DOUBLE</code>: Playback moves to the second loop when it next reaches the end of the first loop.</li>
<li>All other resident samples, including <code>SINGLE</code>, the Amiga modes and samples without a loop: the note ends
immediately and the <code>OnStop</code> notification is delivered.  In oversampling mode the audible output fades out.</li>
<li>Streams: playback leaves the stream loop and drains the remaining source data up to the loop end, then finishes.
A stream without a loop is unaffected.</li>
</list>
<p>A release applies to the current playback only.  The sample's loop configuration is never modified, and the next <function>MixPlay</function> starts unreleased in the first loop.  If the channel is paused, the release is retained and takes effect when <function>MixContinue</function> resumes it.  A channel that is already released, stopped or finished is unaffected.</p>
<p>On worker backends, <code>Okay</code> reports queue admission; the command is applied before a subsequent render period.</p>
    </description>
    <result type="ERR">
      <error code="Okay">Operation successful.</error>
      <error code="OutOfRange">A value is outside of the valid range</error>
      <error code="BufferOverflow">A buffer overflow has occurred</error>
      <error code="NotInitialised">The resource has not been initialised</error>
      <error code="NullArgs">Function call missing argument value(s)</error>
    </result>
  </function>

  <function>
    <name>MixSample</name>
    <comment>Associate a sound sample with a mixer channel.</comment>
    <prototype>ERR snd::MixSample(objAudio * Audio, INT Handle, INT Sample)</prototype>
    <tiriPrototype>err = MixSample(Audio:obj, Handle:num, Sample:num)</tiriPrototype>
    <tags>mutates-object</tags>
    <input>
      <param type="objAudio *" name="Audio">The target Audio object.</param>
      <param type="INT" name="Handle">The target channel.</param>
      <param type="INT" name="Sample">A sample handle allocated from <class name="Audio" method="AddSample">Audio.AddSample()</class> or <class name="Audio" method="AddStream">Audio.AddStream()</class>.</param>
    </input>
    <description>
<p>This function will associate a sound sample with the channel identified by Handle.  The client should follow this by setting configuration details (e.g. volume and pan values).</p>
<p>The referenced Sample must have been added to the audio server via the <class name="Audio" method="AddSample">Audio.AddSample()</class> or <class name="Audio" method="AddStream">Audio.AddStream()</class> methods.</p>
    </description>
    <result type="ERR">
      <error code="Okay">Operation successful.</error>
      <error code="NoData">The sample handle refers to a dead or unconfigured sample.</error>
      <error code="OutOfRange">A value is outside of the valid range</error>
      <error code="DataSize">The sample has an invalid length.</error>
      <error code="NullArgs">Function call missing argument value(s)</error>
    </result>
  </function>

  <function>
    <name>MixStop</name>
    <comment>Terminates playback on a channel.</comment>
    <prototype>ERR snd::MixStop(objAudio * Audio, INT Handle)</prototype>
    <tiriPrototype>err = MixStop(Audio:obj, Handle:num)</tiriPrototype>
    <tags>mutates-object</tags>
    <input>
      <param type="objAudio *" name="Audio">The target Audio object.</param>
      <param type="INT" name="Handle">The target channel.</param>
    </input>
    <description>
<p>This function ends the current playback on a channel.  The stop is terminal: the channel cannot be resumed with <function>MixContinue</function>, and a new playback must be started with <function>MixPlay</function>.  The channel's sample selection and loop configuration are unaffected.  Output stops at the next render boundary without a fade-out.</p>
<p>If the channel is playing, released or paused, the sample's <code>OnStop</code> notification is delivered once for the ended playback.  Stopping a channel that has already stopped or finished has no effect and delivers no notification.</p>
<p>On worker backends, <code>Okay</code> reports queue admission; the command is applied before a subsequent render period.</p>
    </description>
    <result type="ERR">
      <error code="Okay">Operation successful.</error>
      <error code="OutOfRange">A value is outside of the valid range</error>
      <error code="BufferOverflow">A buffer overflow has occurred</error>
      <error code="NotInitialised">The resource has not been initialised</error>
      <error code="NullArgs">Function call missing argument value(s)</error>
    </result>
  </function>

  <function>
    <name>MixSubmitBatch</name>
    <comment>Submits an independently owned batch of mixer commands.</comment>
    <prototype>ERR snd::MixSubmitBatch(objAudio * Audio, const std::span&lt;const struct AudioMixCommand&gt; &amp;Commands, FUNCTION * OnComplete)</prototype>
    <tiriPrototype>err = MixSubmitBatch(Audio:obj, Commands:array, OnComplete:func)</tiriPrototype>
    <tags>mutates-object, copies-input</tags>
    <input>
      <param type="objAudio *" name="Audio">The target Audio object.</param>
      <param type="std::span&lt;const struct AudioMixCommand&gt;" name="Commands">Commands to copy into one channel set's queue.</param>
      <param type="FUNCTION *" name="OnComplete">Optional completion callback; pass NULL when no execution acknowledgement is needed.</param>
    </input>
    <description>
<p>This function submits a list of mixer commands in a single batch.  <code>Commands</code> is an array of <st>AudioMixCommand</st> records.  Submission takes a copy of the complete array, or rejects it without publishing any commands.</p>
<p>All commands must address live channels in the same channel set.  At most 1023 commands can be submitted at once; the set's queue holds 1024 entries, including one internal boundary entry per batch.  Invalid command types, channels or numeric arguments reject the entire batch.  Sample availability and playback state are checked at execution time.</p>
<p>Batches execute in submission order within their set, with one batch per mixer update boundary as determined by <function>MixTempo</function>.  Commands execute in array order without rendering frames.  Different sets have independent update boundaries.  On worker backends, individual Mix calls use a separate FIFO which is drained before rendering each period (and by synchronous Sound queries); they can overtake queued batches, even when submitted later.  On backends without a worker, individual Mix calls execute immediately.</p>
<p>Successful submission means queue admission, not successful playback or audible output.  A failed command does not roll back earlier commands or prevent later commands in the batch from running.</p>
<p>The optional <code>OnComplete</code> callback receives <code>(Audio, Error, FailedCommand)</code> once all commands have executed or been cancelled.  <code>Error</code> is <code>Okay</code> on success and <code>FailedCommand</code> is -1.  Otherwise they identify the first failing command's error and zero-based array index.  Completion acknowledges application to mixer state, not audible output.  Native callbacks have the signature <code>void Callback(objAudio *Audio, ERR Error, int FailedCommand, APTR Meta)</code>.</p>
<p>Callbacks run through the client event loop, outside the mixer mutex, never inline during submission or on the render worker.  They may submit more batches or deactivate Audio.  Notifications follow completion order; batches within a set execute FIFO, while different sets have independent boundaries.  Use a closure or native callback metadata to associate the notification with caller-owned batch state.  No callback is delivered for a rejected submission.</p>
<p>Callback storage is reserved before admission.  Up to 1024 batches with callbacks may be awaiting execution or delivery per Audio object; exhaustion rejects admission with <code>BufferOverflow</code>.  Delivery releases storage automatically.  A <code>NULL</code> callback uses no completion slot.  The event loop must run to deliver notifications.</p>
<p>Closing a channel set or deactivating Audio cancels its queued batches with <code>Cancelled</code> and failed index zero. Already completed results remain unchanged.  Notifications remain deliverable after deactivation and are independent of channel lifetime.  Removing a sample drains the single-command FIFO, then invalidates queued batch sample selections referencing it: they fail with <code>NoData</code> even if the sample slot is reused.  Other batch commands continue. Keep samples alive until completion when successful playback is required.</p>
<p>The callback context is weakly pinned for stale-reference detection.  Destruction of that context suppresses delivery. Freeing Audio discards pending callbacks without invoking application code; deactivate Audio and process notifications before freeing it if cancellation reporting is required.  Device recovery may delay execution until it succeeds or shutdown cancels the queued batches.</p>
    </description>
    <result type="ERR">
      <error code="Okay">Operation successful.</error>
      <error code="Args">Invalid arguments passed to function</error>
      <error code="OutOfRange">A value is outside of the valid range</error>
      <error code="SystemLocked">Part of the system is unreachable due to a persistent lock</error>
      <error code="BufferOverflow">A buffer overflow has occurred</error>
      <error code="NotInitialised">The resource has not been initialised</error>
      <error code="InvalidState">Object was in an incorrect state for the operation</error>
      <error code="NullArgs">Function call missing argument value(s)</error>
    </result>
  </function>

  <function>
    <name>MixTempo</name>
    <comment>Sets the tracker tempo for a channel set.</comment>
    <prototype>ERR snd::MixTempo(objAudio * Audio, INT Handle, INT Tempo)</prototype>
    <tiriPrototype>err = MixTempo(Audio:obj, Handle:num, Tempo:num)</tiriPrototype>
    <tags>mutates-object</tags>
    <input>
      <param type="objAudio *" name="Audio">The target Audio object.</param>
      <param type="INT" name="Handle">The channel set allocated from OpenChannels(), or any live channel in that set.</param>
      <param type="INT" name="Tempo">Tracker tempo from 1 to 100000 BPM inclusive.</param>
    </input>
    <description>
<p>Sets the tempo in beats per minute (BPM), with 24 mixer ticks per beat.  Each tick is one channel-set update boundary at which one queued batch can execute.  The default tempo is 125 BPM: 50 ticks per second, or 20 ms per tick before frame rounding.  This setting affects every channel in the selected set.</p>
<p>The ideal tick length is OutputRate * 2.5 / Tempo frames.  Round to the nearest even frame count, with exact odd-frame ties rounded upwards, then enforce a minimum of two frames.  Equivalently:</p>
<pre>frames = max(2, 2 * floor((floor(OutputRate * 5 / (Tempo * 2)) + 1) / 2))</pre>
<p>The actual tick duration is frames / OutputRate seconds.  At 125 BPM, output rates of 22050, 44100 and 48000 Hz produce 442, 882 and 960 frames respectively.  At 44100 Hz and 128 BPM, each tick contains 862 frames.</p>
<p>A change preserves the frames remaining in the current tick.  The next tick scheduled after the command executes uses the new tempo.  A tempo command inside <function>MixSubmitBatch</function> therefore controls the interval immediately following that batch; if several tempo commands occur in a batch, the last one wins.  Worker-backed calls return <code>Okay</code> on queue admission and apply the command before a subsequent render period (or when a synchronous query drains the queue).  Already rendered audio is unaffected.  Other backends apply the command immediately.</p>
    </description>
    <result type="ERR">
      <error code="Okay">Operation successful.</error>
      <error code="OutOfRange">A value is outside of the valid range</error>
      <error code="BufferOverflow">A buffer overflow has occurred</error>
      <error code="NotInitialised">The resource has not been initialised</error>
      <error code="NullArgs">Function call missing argument value(s)</error>
    </result>
  </function>

  <function>
    <name>MixVolume</name>
    <comment>Changes the volume of a channel.</comment>
    <prototype>ERR snd::MixVolume(objAudio * Audio, INT Handle, DOUBLE Volume)</prototype>
    <tiriPrototype>err = MixVolume(Audio:obj, Handle:num, Volume:num)</tiriPrototype>
    <tags>mutates-object</tags>
    <input>
      <param type="objAudio *" name="Audio">The target Audio object.</param>
      <param type="INT" name="Handle">The target channel.</param>
      <param type="DOUBLE" name="Volume">The new volume for the channel.</param>
    </input>
    <description>
<p>This function will change the volume of the mixer channel identified by Handle.  Valid values are from 0 (silent) to 1.0 (maximum).</p>
    </description>
    <result type="ERR">
      <error code="Okay">Operation successful.</error>
      <error code="NullArgs">Function call missing argument value(s)</error>
    </result>
  </function>

  <types>
    <constants lookup="EQB" comment="Parametric equaliser band types.">
      <const name="HIGH_PASS">High-pass filter; gain is ignored.</const>
      <const name="HIGH_SHELF">High-frequency shelf with gain in decibels.</const>
      <const name="LOW_PASS">Low-pass filter; gain is ignored.</const>
      <const name="LOW_SHELF">Low-frequency shelf with gain in decibels.</const>
      <const name="PEAK">Bell filter with gain in decibels.</const>
    </constants>

    <constants lookup="LOOP" comment="Loop modes for the AudioLoop structure.">
      <const name="AMIGA">Single loop: Amiga style.</const>
      <const name="AMIGA_NONE">Amiga loop: Do nothing.</const>
      <const name="DOUBLE">Double loop: When the note is released, playing shifts to the second loop.</const>
      <const name="SINGLE">Single loop: Releasing will end the note.</const>
      <const name="SINGLE_RELEASE">Single loop: Sample data after the loop will be played when the note is released.</const>
    </constants>

    <constants lookup="LTYPE" comment="Loop types for the AudioLoop structure.">
      <const name="BIDIRECTIONAL">The sample will play in reverse whenever it hits the end marker, then forwards when it hits the start marker.</const>
      <const name="UNIDIRECTIONAL">The sample playback position returns to the byte position specified in the Loop1Start field.</const>
    </constants>

    <constants lookup="MIX" comment="Mixer batch operations.">
      <const name="CONTINUE">Resume a paused channel; arguments ignored.</const>
      <const name="FREQUENCY">Set playback frequency using Integer (0 to 192000 Hz).</const>
      <const name="MUTE">Set mute using Integer (zero is unmuted).</const>
      <const name="PAN">Set pan using Value (-1 to 1, clamped).</const>
      <const name="PAUSE">Suspend playback, preserving position, loop and release state; arguments ignored.</const>
      <const name="PLAY">Start playback at byte position Integer (non-negative).</const>
      <const name="RELEASE">Release the playing note according to the sample's loop mode; arguments ignored.</const>
      <const name="SAMPLE">Select sample handle Integer (positive).</const>
      <const name="STOP">End playback terminally; arguments ignored.</const>
      <const name="TEMPO">Set channel-set tracker tempo using Integer (1 to 100000 BPM; 24 ticks per beat).</const>
      <const name="VOLUME">Set gain using Value (0 to 1, clamped).</const>
    </constants>

  </types>
  <structs>
    <struct name="AudioEQBand" comment="One parametric equaliser band.">
      <field name="Type" type="EQB" lookup="EQB">Filter type</field>
      <field name="Frequency" type="DOUBLE">Centre or corner frequency in hertz</field>
      <field name="Gain" type="DOUBLE">Band gain in decibels</field>
      <field name="Q" type="DOUBLE">Quality factor or shelf slope</field>
    </struct>

    <struct name="AudioLoop" comment="Loop settings for the AddSample() method.">
      <field name="LoopMode" type="LOOP" lookup="LOOP">Loop mode (single, double)</field>
      <field name="Loop1Type" type="LTYPE" lookup="LTYPE">First loop type (unidirectional, bidirectional)</field>
      <field name="Loop2Type" type="LTYPE" lookup="LTYPE">Second loop type (unidirectional, bidirectional)</field>
      <field name="Loop1Start" type="INT64">Byte position at the start of the first loop</field>
      <field name="Loop1End" type="INT64">Byte position at the end of the first loop</field>
      <field name="Loop2Start" type="INT64">Byte position at the start of the second loop</field>
      <field name="Loop2End" type="INT64">Byte position at the end of the second loop</field>
    </struct>

    <struct name="AudioMixCommand" comment="One command in a caller-owned mixer batch.">
      <field name="Operation" type="MIX" lookup="MIX">Mixer operation</field>
      <field name="Handle" type="INT">Target channel; all commands in a batch must use the same channel set</field>
      <field name="Integer" type="INT64">Integer argument for the selected operation; otherwise ignored</field>
      <field name="Value" type="DOUBLE">Finite floating-point argument for PAN and VOLUME; otherwise ignored</field>
    </struct>

  </structs>
</book>
